plugins {
    id 'java'
}

repositories {
    mavenCentral()
}

dependencies {
    // Usamos directamente JUnit Jupiter (JUnit 5)
    testImplementation "org.junit.jupiter:junit-jupiter:5.11.0"
}

test {
    // Necesario para que Gradle ejecute con JUnit 5
    useJUnitPlatform()
}
